# Session journal — 2026-09-18: from a plateau to a corrected search

Engineer: Hermes agent. Same policy of record as `RL_JOURNAL.md`: every claim below was measured.
Where a hypothesis died, the number that killed it is included — the refutations are the useful part.

---

## 0. State at the end of this session

| item | value |
|---|---|
| Live endpoint | `https://survival.zaitzev.com/predict` — UpCloud **Helsinki** 212.147.239.222, Caddy TLS → container `nac-survival-vps:9052` |
| Best validation score | **850.28** (104 s, no errors) — first score produced by a controller we can reproduce offline |
| Deployed controller | `best_controller.py` sha256 `d60bb466…`, single canonical copy, provenance-gated at build |
| Offline mean survival (Linux) | **8,433 ticks** on the evolution seed set (score ≈ ticks/10) |
| Current activity | parallel evolutionary search on the VPS, 5 workers, 3 of 4 cores, nice 19 |
| Untouched during validation | endpoint was never restarted; latency measured before/after every infra change |

Score mechanics, verified from source and unchanged: `score ≈ sim_time/10`; `score += dt` once per
step (dt=0.1) regardless of agent count; `+ fruit.energy/1000` on eating; `− agent.energy/100` when a
predator eats an agent. Official score is the **mean of 3 runs** on preset seeds.

---

## 1. Discovery: production was never running the code we were tuning

The repo held **two** `best_controller.py` files with byte-identical `params.json`:

```
best_controller.py             (repo root)   -> COPYed into the Docker image, served by the VPS
experiments/best_controller.py              -> the file every parameter sweep actually tuned
```

The root copy had `grep -c repro_energy_abs` = **0** and `relay_age` = **0** — none of the relay /
absolute-energy-gate work existed there. So every leaderboard score of that period
(580.96 / 655.95 / 771.8 / 860.2 / 631.06) was produced by a controller nobody had benchmarked, and
the "improvements" we were measuring could never have reached the grader.

An A/B of the two copies on 8 seeds (identical params) measured them as **behaviourally different —
5/8 seeds diverged**:

```
seed   ROOT(live)   EXP(tuned)   delta
100      5286         4674        -612
200      5454         5320        -134
300      9676         9130        -546
400      6558         6558           0
500      3620         3620           0
600      5768         7997       +2229
700     10345        11882       +1537
800      5826         5826           0
ROOT mean 6566.6 median 5797.0    EXP mean 6875.9 median 6192.0
```

The root copy also lacked `reset_memory()`, so it carried per-agent state **across episodes inside one
process** — its score depended on seed *ordering*, which the grader controls and we cannot reproduce.

**Fix (committed `540cee2`):** root is the single canonical file; the duplicate is deleted; the
`experiments/` → params path is a **symlink** so it cannot drift independently; harnesses assert both
`reset_memory` presence and that the loaded module IS the canonical path; `tools/check_controller.py`
guards single-source + a recorded sha256; `Dockerfile.vps` runs `sha256sum -c` so a build refuses to
ship anything but the reviewed file. The build log confirms the gate: `best_controller.py: OK`.

---

## 2. Discovery: the harness was nondeterministic, so most tuning signal was noise

Same seed, same controller, same params — **four runs, four answers**: `601.6 / 626.1 / 611.0 / 678.3`.
Three different harnesses disagreed on seed 100: `4674 / 5363 / 5368`.

Isolation procedure that found it:
1. **Constant policy** (zero state) → identical ×3 in one process and across two processes ⇒ the sim
   looked clean.
2. Instrumented the policy's module state: `_MEM/_GC/_AGE/_EPOCH` all cleared at episode start, and
   `_GC`/`_AGE` were empty at the first policy call ⇒ not policy-state leakage.
3. Recorded a **per-tick fingerprint** of the whole world and diffed two runs. First divergence: one
   agent's *position* differed at tick ~1,200 while its energy and age were **identical** — i.e. the
   same input produced a slightly different action.

Root cause: `_get_local_fruits` / `_get_local_objects` (`environment.py:157-207`) return **sets of
entity objects**, and Python hashes objects by identity, so iteration order followed **memory
addresses**. That order flows into (a) the observation list the policy reads and (b) the fruit-eating
loop (`for fruit in reversed(local_fruits)`) — so float summation order differed per episode, and
chaos amplified it. A stateless policy is immune, which is exactly why every earlier determinism test
passed while the real policy did not.

**Fix (committed `74d3ea1`):** sort those lists by position in our local `src/`
(`creature.py` process_objects + hit_edges, `environment.py` fruit loop). After: **462.3278 ×4
in-process and ×4 in a fresh process.** This is a measurement fix in our copy — it changes our
numbers only, never the grader's world.

---

## 3. The energy ledger: where the fleet's energy actually goes

Reconstructed from the policy's own chosen actions plus the sim's score deltas
(`score_delta = 0.1 + fruit_energy/1000 − victim_energy/100`), seed 100:

```
bucket      n_mean | metabolism   move  spawns |  BURN  | income |    net
    0-1000    10.9 |       1092   1945    1800 |  4837  |  7755  |  +2918
 1000-2000    13.9 |       1394   2792    1200 |  5386  |  9995  |  +4608
 2000-3000    15.3 |       1526   2995    2100 |  6621  |  9689  |  +3068
 3000-4000    12.6 |       1263   3335    1900 |  6498  |  2947  |  -3551
 4000-5000     7.6 |        760   1715    1200 |  3675  |  -527  |  -4201
 5000-6000     2.7 |         97    146       0 |   243  |  -552  |   -795

TOTAL metabolism=6133 (22%)  movement=12928 (47%)  spawns=8200 (30%)  BURN=27261
TOTAL income=29308  ->  NET=+2047
```

Three findings: (1) **income collapses after t≈3,000** and goes *negative* after t≈4,000 — the fleet
dies of an income famine, not of old age; (2) the fleet **spends its surplus in the boom** and enters
the famine with nothing; (3) **spawns are 30% of all energy burned** for a population that adds
nothing to `score` (which is per-tick, count-independent) while each extra agent adds 0.1/tick
metabolism. d=`0.1`/tick, biome `energy_drain_rate` is 1.0 everywhere, so metabolism is exactly the
score rate **per agent**.

---

## 4. Discovery: income is ACCESS-limited, not supply-limited

Direct measurement from the live world (deterministic harness), final 2,000 ticks:

```
seed 100 (4,472 ticks):  standing fruit = 4,518 energy (89.6 fruits) | fleet energy = 1,109 | income +287
                         fruits visible per agent = 0.3 | ticks with NO fruit visible = 576/1998 (29%)
seed 300 (6,828 ticks):  standing fruit = 3,663 energy (75.4 fruits) | fleet energy = 2,173
                         fruits visible per agent = 0.6 | ticks with NO fruit visible = 249/1998 (12%)
```

The world holds **3-4× more food energy than the entire fleet owns**, the fleet captures almost none
of it, and in a sixth to a third of the endgame ticks **no agent can see a single fruit**. The fleet
starves next to food it cannot find — a much better problem than a supply ceiling, because it is a
search problem.

---

## 5. What was refuted today (with the numbers)

Held-out EVAL seeds (1000-1700) were never used for tuning. A result only counts if it survives them.

| hypothesis | train-seed result | held-out result | verdict |
|---|---|---|---|
| small fleet saves energy (target 4 / 2) | 4052 / 2722 vs 6629 | — | **REFUTED** — the crowd IS the foraging engine (fruit eaten 1041→334→185) |
| bank energy + few agents (abs300/target4) | 4508 vs 6629 | — | **REFUTED** |
| bank medium (abs200/target6) | 6359 vs 6629 | — | no gain (within noise) |
| chase trees (`tree_weight` 1/3/8) | 6224/6254/3938 vs 6818, food captured 846/749/443 | 6651 vs 7206 (−555) | **REFUTED** — trees are a trap; travel costs more than it earns |
| faster blind movement (blind 0.7) | 6051 vs 6818 | 6936 vs 7206 (−270) | **REFUTED** |
| larger population (target 16 / 20 / 26) | 7358 / 7260 / 7758 vs 6818 | target 26: 6189 vs 7206 (−1017) | **REFUTED** — more income, but predation cost eats it |
| imitation learning | — | mean 1307 vs expert ~6000 on held-out seeds | **REFUTED** — expert depends on internal state absent from the 31-dim observation (spawn cooldown, flee hysteresis, heading stickiness, population estimate) |
| from-scratch PPO | 74±9 vs heuristic 210 | — | **REFUTED** (earlier session) |
| relay / age-roles | 5313 / 4928 vs 7492 | — | **REFUTED** (earlier session) |

Pattern worth stating plainly: **every train-seed "win" of ~500-1,500 ticks failed on held-out seeds.**
With 8 seeds and std ≈ 2,000, the standard error is ≈ 700, so effects of that size are not measurable
at that sample size. This is why the harness had to be fixed before any further tuning.

---

## 6. Discovery: macOS and Linux produce different trajectories

Same seed, same controller: seed 100 → **7,816 ticks on Linux (VPS)** vs **4,472 on macOS (ARM)**.
Floating-point/library differences get amplified by the same chaos. The **grader runs Linux**, so:

* Linux is the authoritative measurement platform (`python:3.12-slim` + numpy/scipy/shapely/pygame,
  `SDL_VIDEODRIVER=dummy`);
* the search now runs there, so fitness matches the grading environment.

---

## 7. Infrastructure (what is where, and why)

* **Serving:** container `nac-survival-vps` on Helsinki, image built from the repo Dockerfile with a
  `sha256sum -c` provenance gate; Caddy terminates TLS (a packaged-unit bug — the Ubuntu `caddy` unit
  omits `--adapter caddyfile`, so Caddy silently served HTTP-only and 443 hung — is documented in the
  hosting skill).
* **Search:** container `nac-evolve` on the same box, `--cpus=4`, `--memory=6g`, workers at `nice 19`.
  Serving latency was measured before and after: **p50 0.089 → 0.086 s**, i.e. no measurable impact.
  Isolation is a design requirement, not an accident: a saturated box could slow a grader call past
  the 10 s per-response limit and contaminate a validation.
* **Compute/results path:** `/opt/nac-compute` (sim, controller, measurement code, evolution outputs).
* **Provenance:** `tools/check_controller.py` currently reports a deliberate mismatch, because the
  controller gained new parameters after the deployed hash was recorded. Deploying requires
  `--update`; the Docker build refuses to ship an unreviewed controller meanwhile.

---

## 8. Methodology rules that earned their place today

1. **Hold out seeds and refuse to believe train-seed wins.** Six hypotheses died this way; shipping
   any of them on the train number would have been a regression.
2. **Resample fitness seeds per generation** during search — a fixed small seed set invites the exact
   overfitting that produced those false wins.
3. **Prove determinism before trusting a ranking** — and prove it with the *real* policy. A constant
   policy passes while the real policy fails.
4. **Localise divergence with a per-tick fingerprint**, not by intuition.
5. **One heavy job at a time, launched in the background.** A foreground tool timeout killed a
   completed first config; the skill warns about this and it still happened.
6. **Measure before and after any infra change** that could touch the live path (latency before/after
   the search; connection count before a redeploy).
7. **Never overwrite the deployed artifact from a search process.** The old `evolve.py` wrote straight
   into `best_controller/params.json`; a crash mid-run could have mutated what the grader scores.
   `evolve_det.py` writes only `evolve_det_best.json`; adoption is a separate explicit step.

---

## 9. The search now running, and how its result will be judged

```
evolve_det.py    (mu+lambda) ES, 22 parameters, sparse Gaussian mutation with annealed step
                 population 20, elite 6, horizon 9000, 3 seeds resampled per generation
                 from the 8 TRAIN seeds; episode-level task packing; 5 workers
                 incumbent PROTECTED: best can never score below the parameters already served
fitness          mean survival ticks (score ≈ ticks/10, and ticks is the cleaner signal)
budget           180 minutes
outputs          /opt/nac-compute/experiments/evolve_det_best.json + evolve_det_history.jsonl
```

Why this is worth doing at all: every number in the deployed `params.json` was selected against a
harness that **could not reproduce itself**, so the current parameterization is unoptimized under the
true deterministic objective. That is a genuine, specific reason to expect gains — unlike the ten
single-knob hypotheses, which were testing mechanisms one at a time.

Decision rule agreed in advance: the winner is re-measured on **held-out seeds 1000-1700** at full
horizon on Linux, and must beat the incumbent by **more than the seed-noise band (~±500 ticks)** to
be worth a redeploy. Anything smaller is not a result.

---

## 10. Next, if the search confirms a winner

1. Confirm on held-out seeds (as above); only then adopt into `params.json`.
2. `tools/check_controller.py --update`, rebuild, redeploy, verify container hash, then a leaderboard
   validation — remembering a single validation is one chaotic draw, so judge against the ~700-800
   mean-of-3 band rather than any individual number.
3. Only after that: the ML track (collect diverse trajectories → imitation → PPO warm-start). It is
   deliberately second: training against a heuristic whose parameters were never properly optimized
   would leave us unable to attribute any gain to learning rather than to fixing bad constants.

---

## 11. The phase-aware (boom/famine) policy — built, then REFUTED by its own ablations

The target moved to **~1,800 score (~18,000 ticks, 2.2x current)**. The arithmetic for that is
sobering and worth recording, because it explains why thrift cannot get there:

```
to hold one survivor for 12,000 ticks after the famine, metabolism alone (0.1/tick) drains ~1,200
energy -- more than the DEFAULT max_energy cap of 500, and only just under the mutated cap of 1000.
So a survivor must ARRIVE at the famine full, and max_energy (heritable, mutates +/-50%) is what
sets how long it can outlast the food.
```

Built: a boom→famine policy in the canonical controller (all inert by default, proven by re-running
the pre-patch seed to the same `462.3278` on macOS and `7816 ticks` on Linux), consisting of
(a) **sim-time reconstruction** — the state carries NO clock, so time is recovered as
`sim_tick = max(birth_tick + calls_since_birth)` per agent, using `age` (sim-seconds, 1 tick = 0.1 s)
for the initial cohort; validated against ground truth at **max error 1 tick**;
(b) a **time-primary famine ramp** with an optional adaptive food-availability guard;
(c) famine-specific movement and reproduction rules.

Results on Linux (the grader's platform), seed 100:

```
baseline (phase OFF)       7816 ticks
phase on lo=4000 hi=7500   4784     -3032
phase on lo=3000 hi=6000   3988     -3828
phase on lo=5000 hi=9000   6140     -1676
```

The bundle coupled movement + reproduction, so it was ablated (movement held EXACTLY at baseline):

```
A0 baseline                                    7816
A1 spawn-gate only (pop<=3, bank 0.9)          4719
A5 spawn-gate, bank relaxed to 0.5             4719   <- bank NOT binding
A6 spawn-gate, bank removed entirely           4720   <- bank NOT binding
```

Since relaxing and then removing the bank requirement changed nothing, the binding condition is the
**population cap itself**: at tick 4,000+ the fleet holds 10-16 agents, so the gate forbids all
reproduction — and because agents are mortal by age (600-1,200 tick lifetimes), a fleet that stops
reproducing dies out. Both halves of the phase policy therefore failed, and for the same underlying
reason:

* **Cutting movement kills income** — food is ACCESS-limited (75-90 fruits standing while agents
  starve); travel is how it is found, so movement is the income mechanism, not discretionary burn.
  Interpreting "movement = 47% of burn" as waste was the error.
* **Cutting reproduction kills the relay** — mortality is by age, so births are load-bearing.

**Consolidated picture after ~12 experiments:** the fleet is **throughput-limited, not
expenditure-limited**. It needs many mobile foragers and continuous reproduction; every thrift
measure reduces income faster than it reduces cost. The plateau is a ceiling on captured energy per
tick against a production curve that decays as `0.5^(t/300)`, and 18,000 ticks requires a genuine
increase in capture efficiency — which no hypothesis tested so far has achieved, and which the
parameter search may or may not find.

## 12. Search modification worth recording: the horizon cap was limiting discrimination

At horizon 9,000 the fitness saturates: generation 4's winner scored 8,661.3 vs the incumbent's
8,433.3 with per-seed `[7984, 9000, 9000]` — **two of three seeds capped**, so the gain was a LOWER
BOUND and further improvements would be invisible (all capped candidates tie). Fix: horizon raised
to 12,000 for headroom, plus **fruit eaten as a tie-breaker** in the ranking (capped candidates still
differ in what they ate, and that is the income driving survival). The search was resumed from the
generation-4 winner rather than restarted, so no progress was discarded.


## 12. Memory-based search (optimal foraging) — implemented, tested, LOSING (unproven)

Research grounding: our controller was a MEMORYLESS potential field. Foraging ecology says that is
the wrong shape for a patchy world: area-restricted search (slow down inside a patch), ballistic/
Levy relocation (commit to a straight run after failing), social foraging, patch memory (Charnov's
marginal value theorem). Independently, the ML track's failure had already identified the missing
state: behaviour cloning died because the observation is NOT Markov, and what was missing is
"where was the food I saw earlier".
Implemented (off by default): `memory_search`, `commit_len`, `commit_speed_frac`,
`follow_agent_weight`, `ars_speed_frac`.

Result, 20 held-out seeds, horizon 12000, paired in one process:
  LIVE 6861.9 ticks / 1047.2 fruit | P1 memory-only 5864.4 / 860.6 (-14.5%)
  P2 memory+social 4219.2 / 507.7 (-38.5%) | P3 full trio 4363.1 / 488.6 (-36.4%)

STATUS: **unproven / losing.** Do NOT record this as "-14.5% explained by the burn".

## 13. Corrections from the independent review (deleg_ab4d5570)

Accepted, with the arithmetic corrected:
  * BURN FIGURE WAS WRONG BY 2x. walking_cost=0.05, sprinting_cost=0.5 and the walking threshold is
    `entity.speed` = 10 (environment.py:501-518), so 10 units/tick costs **0.5 energy/tick ~5x the
    0.1/tick metabolism**, not "1.0 energy/tick, 10x". The live controller walks 4.5 units/tick
    (walk_frac 0.45) = 0.225/tick, so my full-speed commit cost ~2.2x the normal walk. Direction of
    the diagnosis was right; the number presented to the user was not, and P1's loss is therefore
    NOT attributable to the burn alone.
  * ARS WAS MIS-APPLIED. Because `direct_forage` is true whenever any fruit is visible, the ARS
    clamp slowed the DIRECT APPROACH to a fruit already being targeted. That cannot keep an agent
    "inside a patch" and only throttles the movement the journal identified as the income
    mechanism. P3 is therefore uninterpretable rather than informative.
  * NO ABLATION ARMS. P2/P3 combined three changes with no social-only or ARS-only arm, violating
    this project's own rule. The "clumping" explanation was post-hoc.
  * "BANKING NEVER FIRES" WAS OVER-CLAIMED. Thresholds were 8000/10000/12000, not 10000-15000, and
    the baseline itself scores 9236/8938 on some seeds -- above B1's 8000. Only B3 (12000) is
    provably never firing. Restated: B1 fires on <=2 seeds, B3 never.
  * RESULTS WERE NOT PERSISTED. They existed only in VPS container logs (docker logs nac-*), so a
    fresh session could not resume. Now transcribed to experiments/paired_results.jsonl.
  * PROVENANCE GATE WAS BROKEN: best_controller.sha256 held 5c8beb20 while the file hashed
    d76d21e8, so the Docker build would fail. Re-recorded with the change committed in the same
    step and the default-off inertness stated explicitly.
  * evolve_det.py OBJECTIVE MISMATCH FIXED: ranking used ticks + 0.2*fruit but adoption used ticks
    only, so a fruit-winning candidate could be logged "no gain" and the saved artifact could be
    worse than the ranked winner. Both now use ticks + FRUIT_W*fruit, with FRUIT_W = 0.05 (small,
    because the fruit term has a HEADCOUNT BIAS: bigger fleets eat more total fruit by having more
    mouths, which would reward the over-breeding trap).

## 14. Banking, final

B1/B2/B3 (switches 8000/10000/12000, all with the v2 relay-rescue semantics) measured 6819.6 /
6756.0 / 6877.3 against a live baseline of ~6952.6 at the same seeds: no gain, and B3's per-seed
list is near-identical to the baseline's because its switch never fires. The route is REFUTED as
the path to 1800, and the reason matters more than the refutation: **runs end in the MIDGAME
(~6900 ticks), before any endgame switch can act.** B4_early_4500 (window 4500-6500) re-tests the
movement-cut component that the earlier phase ablation already measured as a big loser
(4000-7500 -> 4784, -3032) and should be read as a repeat, not a new test.

## 15. Where we actually stand

  * Deployed controller (live params): ~6862-7206 ticks across two independent measurements
    (this session's 20-seed paired run; and the earlier 8-seed `E0 baseline target12` entry at
    bench_std_results.jsonl:72). Nothing tested today cleared the +500-tick held-out bar.
  * Best candidate of the day: H1 (winner's foraging + live's spending) at +314 ticks (+4.5%),
    below the bar.
  * The live controller is a genuine VPS evolution product: gen0 356.97 -> gen6 679.44
    (best_controller/evolve_history.json), i.e. +90% over the pre-evolution baseline, and it beats
    DEFAULT_PARAMS by ~3x on held-out seeds. That gain is real and is what is serving.


## 16. THE MECHANIC THAT ACTUALLY ENDS RUNS: a predation cascade, not starvation

This reframes everything above. Death-budget diagnostic (experiments/w_deaths.py) on the LIVE
controller, horizon 12000:

  seed 1000  steps 5728  deaths {'eaten': 35, 'starved': 64}  (35% eaten)
    t=1001  19 agents  1 predator  fleet energy 2734
    t=2001  13 agents  1 predator  fleet energy 3143     <- healthy and well banked
    t=4001  16 agents  3 predators fleet energy 2767     <- STILL healthy
    t=5001   2 agents  3 predators fleet energy   51     <- 14 of 16 gone in 1000 ticks
  seed 1001  steps 7102  deaths {'eaten': 72, 'starved': 95}  (43% eaten); same terminal collapse

The fleet banks ~3,000 energy, holds it for thousands of ticks, then loses most of its agents in a
single window -- and each killed agent takes its bank with it (2767 -> 51 is that hole). Predation
is 35-43% of all deaths, and the predator spawn chance GROWS LINEARLY WITH TIME
(environment.py:763: (1/max(1,N_predators)) * dt * time * 0.0001, independent of fleet size).

CONSEQUENCE: runs end in a predation cascade. Every experiment in sections 12-15 -- foraging,
memory search, banking, lean re-evolution -- was aimed at STARVATION, which is the wrong failure.
That is the single best explanation for a day of null results.

## 17. Predator mechanics (read from the sim, and why the exploit fails)

src/elements/predator.py:37 -- a predator CHARGES only when the agent is NOT facing it
(|agent_looking_dir| > pi/2) or when it is already inside hearing_radius*1.5 = 90 units. If the
agent faces it, it PIVOTS 45 degrees off-target. Predators NEVER TIRE (Predator.step sprints with
no energy accounting), so kiting is impossible. Turn cost = |turn|/(2pi) (180deg = 0.5) vs 5.5
energy/tick to sprint away; movement is applied BEFORE the turn (environment.py:614/618).

I implemented face_predator to hold a pursuer inside the facing cone, and it produced BIT-IDENTICAL
episodes to the live controller -- the proof it never fired. Why: _facing_turn needed a predator
visible at 90-320 units OUTSIDE a 60-degree cone, but an agent only sees predators inside its
vision cone (~+-45deg) or within hearing (~100 units, any angle), and a predator charges precisely
when it is BEHIND (invisible). Observability blocks the exploit. Even when facing does engage, the
pivot still closes radially at 15*cos(45deg) ~ 10.6 units/tick, which beats our walk speed of 10,
so facing cannot stop the approach -- only sprinting can, at 5.5 energy/tick.

CONCLUSION: the terminal predation cascade is very likely a hard limit of this action space rather
than something tunable. Kept the knobs (default OFF) and this reasoning for the record.

## 18. H1 -- the one candidate that clears the bar (pending the block-2 baseline)

H1 = the evolution winner's FORAGING genes + the LIVE controller's reproduction discipline
(the winner over-bred: spawn_cooldown 30 vs 120, fleet target 17.6 vs 12, absolute spawn gate 34).

  block 1 (seeds 1000-1019): H1 7266.6 vs live 6952.6  -> +314 (+4.5%)
  block 2 (seeds 1020-1039): H1 7782.4, with runs of 12000/11361/10996/9866/9333 ticks
  pooled (40 seeds)         : H1 ~7524 mean; live baseline for block 2 pending

Deploy bar: >+500 ticks over the live controller across the pooled 40 held-out seeds.


---

## 17. H1+ PLAN: the physics of why the fleet dies, and the lockout experiment (2026-09-18 18:49Z)

### 17.1 Two official H1 samples, and what they prove
- 18:00-18:07Z validation: **768.68**; 18:20-18:25Z validation: **918.33** (0 errors both).
  Same controller, 5 hours apart, +150 score apart. This is the +-130/+150 swing measured
  locally, now confirmed on the OFFICIAL metric. A single before/after validation cannot rank
  two controllers here; only paired runs on identical seeds can.
- Verified server-side: the live log is INSIDE the container at `/app/predict_log.jsonl`
  (18,080 lines, last write 18:25Z) - the file on the host at `/opt/nac-survival/logs/` is the
  preserved PRE-deploy log (last write 17:49Z) and misled an earlier check.
- The final line of the 918.33 run is the diagnosis:
  `{"sim": 896.7, "score": 918.4583, "n": 1, "obs": {"Predator": 1, "Edge": 11}, "emean": 23.4}`
  One agent left, 23.4 energy, a predator adjacent, NO fruit and NO trees in view. The run ended
  the way the source code says it must.

### 17.2 Source-code findings that change the target (all measured today, not assumed)
1. `score = self.time` (environment.py:760 adds dt=0.1/tick; fruit adds only energy/1000 = 0.06
   for a full fruit). The score is TIME ALIVE. Fleet size earns nothing.
2. NO episode time cap exists in the environment - a run ends when the LAST agent dies. 1800 is
   a target, not a cap.
3. THE LOCKOUT (environment.py:512): `if entity.energy < entity.max_energy/5` (i.e. < 100) the
   sim CLAMPS movement to walking speed. Below 100 energy an agent physically cannot sprint.
   Agent sprint is 20, predator sprint is 15 - so a fed agent can always outrun a predator and
   an unfed one can never. **Predation deaths are a starvation symptom.**
4. Reproduction costs the parent 100 energy (environment.py:621-623) and the controller's
   `reserve_frac` gate for sprintings is 0.15 = 75 energy - BELOW the sim's own lockout. So
   spawning drives parents into the zone where the sim forbids the very escape the controller
   thinks it can perform. This is the same class of bug as the old fractional spawn gate.
5. Predators have finite energy (0-200), start asleep, wake above 100, gain 3/tick while resting
   and CANNOT kill while resting. They only CHARGE when the prey is not looking at them
   (|rel_dir| > pi/2) or closer than 90; otherwise they PIVOT 45 deg off, which closes radially
   at 15*cos(45) = 10.6/tick. So retreating WHILE FACING is strictly better than turning away.
6. The world decays: `tree_spawn_chance *= 0.5 ** (time/300)` (halves every 3,000 ticks) and
   fruit rots at age 100. Income trends to zero; the endgame can only be a bank.
7. Biomes are observable in the agent state and differ (forest/grassland 0.1 fruit/s, swamp 0.08
   with 0.5 move penalty, desert 0.05, river 0.0 with 0.3). No low-drain refuge exists.

### 17.3 Research (how others solve this class of problem)
- SELFish (Hahn et al., ALIFE 2019): prey trained on "+1 per step alive, -1000 when caught" -
  our exact reward. Their own control measurement: for an INDIVIDUAL's survival the best policy
  was simply TurnAway (flee), which the learned swarm did not find because grouping was a
  self-interested Nash equilibrium.
- Predator-prey survival pressure alone evolves swarming (NJP 2023): no engineered avoidance
  term is needed.
- Dilution of risk / selfish herd: risk is highest on the periphery; the most-hunted individuals
  "are frequently those who break away". Positioning relative to the PREDATOR is a mechanism our
  controller lacks entirely.
- MAP-Elites with adaptive sampling for noisy domains (GECCO 2019): a repertoire of diverse
  elites with re-sampling to beat a noise floor - the right search under our +-1,000-tick noise,
  and it keeps alternatives alive by construction.

### 17.4 Control bug found and fixed before launching (the "wrong baseline" class)
`/opt/nac-phase-test/deployed_params.json` held the PRE-H1 controller (`fruit_weight 2.753`).
Every "LIVE (deployed)" row in today's block runs was therefore the OLD controller, not H1. The
pooled +380 (+5.4%) figure stands - that IS the H1-vs-old-controller number the deploy decision
needed - but any future comparison must use H1 as the reference. Wrote the true H1 params to
`/opt/nac-phase-test/h1_baseline_params.json` (verified identical to what the serving container
loads: fruit_weight 1.1482261798517022, reserve_frac 0.15, repro_energy_abs 0.0) and launched on
that.

### 17.5 Launched: the 2x2 lockout factorial (pure config, no code change)
Arms (all on the H1 baseline): L1 `reserve_frac 0.22` (align the sprint gate ABOVE the sim's
0.20 lockout) | L2 `repro_energy_abs 250` (a parent keeps >=150 after paying 100 to spawn, so it
stays sprint-capable) | L3 both | L4 `reserve 0.22 + abs 350`.
Design: the SAME 4 arms in BOTH containers, on DISJOINT fresh held-out seed halves
(1060-1069 and 1070-1079), horizon 18,000, each container internally paired against the H1
reference arm. Pooling the per-container paired deltas gives 20 paired seeds without
cross-container (unpaired) comparisons.
Predicted effect: removes the mechanism that killed the last agent at 918.33, therefore prevents
the mid-game predation cascade - thousands of ticks, i.e. resolvable above the +-1,000 noise.
ETA: ~55-70 min per container (5 rows x 10 seeds x ~7,500 avg ticks at ~8 ms/tick).
Compute isolation verified: `--cpus=1.5` each, `nice -n 19`, serving latency 0.010-0.025 s under
load versus 0.010-0.036 s idle, load average 0.88.


### 17.6 Review corrections applied (2026-09-18 19:03Z)
A review of §17 rightly flagged overstatement. Corrections now reflected in both this journal and
the plan:
1. "Every birth drops a parent into the lockout zone" -> only true below ~200 pre-spawn energy.
2. "score is simply time alive" -> survival time DOMINATES; fruit/energy terms are small but real.
3. "the endgame can only be a banked coast" -> too confident; standing fruit remained in the world
   while access was the binding constraint, so both sides matter.
4. "facing makes predators close 40% slower" -> unverified AND my arithmetic was wrong (the pivot
   ratio is cos(45 deg) = 0.707, ~29%, before the agent's own movement). To be measured by the
   closing-rate diagnostic, not inferred from source.
5. "MAP-Elites is for our ±1,000-tick noise" -> mischaracterised; the simulator is deterministic
   in a fixed harness and the variance is seed-to-seed. QD is for diversity/robustness across
   seeds, which is also what keeps alternatives alive.
6. "the safest position puts another agent between you and the predator" -> a hypothesis for THIS
   sim, not an established result.
7. "reproduction + lockout explains the ~7k ceiling" -> NOT concluded. The reviewer's list of live
   alternatives (predator avoidance, food accessibility, movement expenditure, population
   structure, reproduction timing, interactions) is the correct framing, and the lockout
   experiment plus the closing-rate/death-cause diagnostic are what will decide it.
Decision rule fixed in advance: promote the lockout fix only on a CONSISTENT improvement across
the 20 paired seeds; otherwise it is a real but secondary mechanic, and we move to the next class.
Also noted for the record: the earlier "LIVE (deployed)" rows in today's block runs were the
PRE-H1 controller, so the pooled +380 (+5.4%) is H1-vs-old-controller (the right number for the
deploy decision) and was never an H1-vs-H1 measurement.


---

## 18. THE LOCKOUT EXPERIMENT DECIDED: the mechanic is real, the fix is HARMFUL (2026-09-18 19:38Z)

### 18.1 Result — 4 arms, 20 paired fresh seeds, horizon 18,000, H1 as the reference
Container A (seeds 1060-1069) and B (1070-1079); the same 4 arms in each, so the per-container
paired deltas are valid:

```
                        A: mean   vs H1            B: mean   vs H1
H1 (deployed, reference)   9068.9      --             7894.4      --
L1 reserve_frac 0.22       7564.5   -1504 (-16.6%)    6988.8   -906 (-11.5%)
L2 repro_energy_abs 250    6848.0   -2221 (-24.5%)    7198.8   -696 ( -8.8%)
L3 both 250                7320.0   -1749 (-19.3%)    6450.2  -1444 (-18.3%)
L4 reserve 0.22 + abs 350  5807.5   -3261 (-36.0%)    5980.4  -1914 (-24.2%)
```

EVERY arm loses in BOTH containers, and the loss is MONOTONE in how hard the arm constrains
energy spending (L4 > L3 > L2). A monotone dose-response across two independent seed sets is not
noise. Under the pre-registered decision rule, the lockout fix is REJECTED.

### 18.2 Why it fails (mechanism, consistent with 4 earlier refutations)
`reserve_frac` does not only gate sprinting: line 481 uses it to throttle movement to 25% speed
below the threshold. Raising it to 0.22 therefore makes MORE agents crawl, which cuts income,
which lowers energy further — a self-defeating loop. And `repro_energy_abs` at 250/350 suppresses
births, breaking the age-mortality relay that keeps the population (the foraging engine) alive.
This is the fifth instance of the same law in this project: the fleet lives at break-even, so any
measure that reduces movement or births loses more income than it saves.

### 18.3 But the diagnostic PROVES the mechanic — and corrects the strategic picture
On H1 at the official horizon (seeds 1060, 1070 protocol, 18,000 cap):
- Death mix: STARVED 49-51%, EATEN 23-32%, AGED 16-27%. Starvation is the plurality cause.
- 77/84 and 33/37 predation deaths were inside the sprint-lockout zone (92%), mean energy at a
  predation death 76.0 / 79.0 (medians 63.7 / 36.0) — predation really does kill the broke.
- 58.1% / 54.4% of all agent-ticks are spent in the lockout zone.
- BUT exposure is rare: only 4.7% / 3.1% of agent-ticks have a predator within 150 units.
- Food ACCESS is the binding constraint, confirmed: 76-87% of agent-ticks are BLIND, agents see
  0.26-0.42 fruits each, while 2,000-4,900 fruit energy stands in the world.
- MEASURED predator closure: while FACING the predator the gap opens 1.583 / 1.359 units/tick
  (n=3911 / 2612); while turned away it is 0.126 open / 0.091 CLOSING (n=1869 / 1078). Facing is
  worth ~12x the separation. My earlier "40% slower closure, inferred from code" was wrong in
  both magnitude and framing — this is why the measurement was demanded.

### 18.4 Revised strategy (evidence-driven, not inferred)
1. The lockout is REAL and predation kills the broke, but protecting the reserve by throttling
   spending LOSES. Drop that axis entirely — 5th refutation, now with a dose-response.
2. The plurality failure is INCOME/ACCESS: agents are blind ~80% of ticks with food standing
   nearby. This is where the next mechanism must act — and the earlier memory-search trial was
   refuted while mis-priced (commit_speed_frac 1.0 => ~2x energy per tick), so it needs a
   properly-priced retest (walk-speed commitment, dispersion-limited following).
3. The facing geometry has a MEASURED mechanism (12x separation) and, unlike the reserve arms,
   it does not require throttling income — so retreat-while-facing is the next arm to test.
4. Calibration note: with H1 as the reference the local harness reads 9068.9 / 7894.4 ticks
   (scores ~907 / ~789) on these 20 fresh seeds, bracketing the two official samples (918.33,
   768.68). The local instrument tracks the official metric.


---

## 19. E3 DEPLOYED — retreat-while-facing evasion goes live (2026-09-18 20:57Z)

### 19.1 The result that justified it (W4, 20 paired seeds, horizon 18,000, H1 reference)
```
seed set 1100-1109   E3 8388.1  vs H1 7468.2   +919.9 (+12.3%)   8/10 seeds
seed set 1110-1119   E3 8260.1  vs H1 7730.3   +529.8  (+6.9%)   5/10 seeds
pooled (20)          E3 8324.1  vs H1 7599.3   +724.9  (+9.5%)  13/20 seeds
```
The two losing arms fail in the PREDICTED direction, which is what separates a mechanism from noise:
E1 (same trigger, 0.8 sprint = 16 units) lost 6-8% -- 16 units is not enough separation against a
predator that sprints 15; E2 (trigger at 220 instead of 140) lost 3-4% -- conceding income before the
threat is real costs more than it saves. So the winning shape is: retreat LATE, at FULL sprint, and only
above 200 energy (a poor agent cannot sustain a sprint anyway -- the sim forbids sprinting below 100).

Against the pre-registered bar: the MEAN passes (+725 > +500), seeds-won MISSES (13/20 vs the 15/20
required). Reported as-is, not rounded up. The user chose to deploy anyway, with a validation.

### 19.2 The deploy, and what was verified
- Code: `best_controller.py` gains one gated block (inert unless `evade_mode > 0`). Unit-verified before
  shipping: OFF reproduces the incumbent episode byte-for-byte; ON fires exactly in the designed case
  (dist 16.0 at 0.8, turn -0.300 to face; correctly skipped at 90 energy and at 300 distance).
- Params: E3's five knobs installed as the shipped `best_controller/params.json`; H1's params preserved
  at `experiments/params_H1_rollback.json`; ledger re-recorded (f90cb4e3...) and the guard reports
  "CONSISTENT: exactly one canonical controller".
- Deploy script `/tmp/deploy_e3.sh` enforces the lessons of the failed H1 deploy: the build's exit code is
  CHECKED and never piped through `tail`, the pre-deploy in-container log is copied out BEFORE the
  restart, and verification reads params + hash OUT of the running container.
- Verified live: IN-CONTAINER PARAMS show evade_mode 1.0 / evade_dist 140 / speed 1.0 / energy 200;
  in-container hash == the reviewed file; GET / 200 in 11 ms; public /predict 9-14 ms.

### 19.3 Caveat raised, to verify: the grader may cap run length
Second-hand note (from another agent's memory) that a run also stops at a ~600 s grader wait cap. Observed
rates tonight: 7,687 ticks in 397 s (19.4 ticks/s) and 9,183 ticks in 333 s (27.6 ticks/s). At those rates
an 18,000-tick run needs 670-900 s, which would exceed a 600 s cap -- implying a practical ceiling near
12,000-16,000 ticks (score 1,200-1,600). UNVERIFIED; if real it redefines the target, so it is worth
pinning down before spending the night on 1,800.

### 19.4 Also in flight
- W3 access arms (matched price) running on the VPS as nac-A1/nac-A2, seeds 1200-1219.
- W2 state-triggered thin relay in development (off-by-default code).
- W5 curriculum spec (research only).
- W1 residual RL training on the Mac with the mid-game curriculum; final confirmation must be on x86 Linux
  because the sim is chaotically platform-sensitive (measured: same seed+policy 4,472 ticks macOS/ARM vs
  7,816 Linux/x86).


### 19.5 CORRECTION: the latency cap had been misread (and it was already in the repo) (2026-09-18 20:59Z)
The repo's own README settles it - this was never hearsay, it was documented and then lost:
  README.md:19   the environment runs until all agents die OR 3,000 simulated seconds = 30,000 ticks
  README.md:165  "the server will wait for responses for up to 10 seconds. If no responses are received
                 within that time or if the ACCUMULATED WAIT TIME reaches 600 seconds, the run will end"
  README.md:186  score ~= min(ticks, 600 s / per-tick latency)
RL_JOURNAL.md:118-122 (17 Sep) had already derived it: "the grader caps ACCUMULATED wait at 600 s, so
score ~= 600s / latency. 110 ms -> ~5.4k ticks (580). 40 ms -> ~15k (1500). 20 ms -> 30k cap (~3000)."

Three consequences:
1. The cap is on OUR response wait, not wall-clock; the grader's own simulation time does not count. At
   the measured 9-14 ms per request the budget for 18,000 ticks is 33 ms/tick, i.e. roughly a 2x margin,
   and the real ceiling is the simulator's own 30,000 ticks (score 3,000). 1,800 is REACHABLE; the
   binding constraint is the policy. The earlier "maybe capped at 1,200-1,600" note in 19.3 was WRONG.
2. It retro-explains the score history: the early attempts scored 78 / 48 / 580, and 580 is almost exactly
   the ~5,400-tick latency ceiling of that era (5,452 requests / 686 s = 110 ms/tick). The climb to 918
   came partly from RAISING the latency ceiling (Helsinki + Caddy + a faster policy), not only from better
   policy. This is why "latency is not a score lever" was true only while the cap stopped binding.
3. New guardrail: more ticks means more accumulated wait, so latency re-binds somewhere past ~20,000
   ticks. Monitor per-tick wait as E3's runs cross ~10,000 ticks (budget: <=33 ms/tick for 18,000).


### 19.6 E3 official validation #1: 798.42 (2026-09-18 21:04Z)
```
enqueued 21:00:39Z, finished 21:03:04Z (145 s), score 798.4244385722203, errors []
=> 7,984 ticks at 18.2 ms/tick
```
Reference samples on the SAME endpoint for the previous controller (H1): 768.68 (19.4 ms/tick) and 918.33
(27.6 ms/tick), mean 843.5. E3's 798.42 sits inside that range, so a single validation neither confirms nor
refutes the local paired +724.9 (+9.5%) / 13-of-20 result. Repeating it is cheap (~2.5 min per attempt) and,
if the leaderboard keeps the best attempt, every extra sample is free upside - so the ask is 3-5 more before
drawing any conclusion. Secondary signal worth noting: E3 measured FASTER per tick (18.2 ms vs 19.4/27.6),
which raises the latency ceiling that bounds long runs (score ~= min(ticks, 600 s / per-tick wait)).

---

## 20. THE OUTSIDE CALIBRATION + THE UNEXPLOITED AXIS (2026-09-18 23:25Z, orchestrator)

### 20.1 We are at the field median, and 1,794 is occupied (not hearsay - public API)
`GET https://cases.nordicaicup.com/api/v1/leaderboard/validation` (75 teams have a survival-simulator score):
1 **1794.35** Cybotrix (IS) | 2 1743.34 Elemental hero (DK) | 3 1712.05 North GreatWall (SE) |
4 1667.59 Iftikhar Amiri (NO) | 5 1661.32 execve (DK) | 6 1633.63 Hannah Family (FI) |
7 1550.18 Childbeating Catboost Connoisseurs (SE) | 8-14 1418.75 / 1402.97 / 1386.41 / 1312.15 /
1300.46 / 1259.17 / 1205.72 | median of the field **927.4**; 39 teams >900, 14 >1200, 7 >1500.
Ours: 918.33 (best official sample), 798.42 (E3). The *evaluation* board (3 runs averaged, ONE attempt) has
only 2 survival entries (529.36, 497.43) - almost nobody has spent their shot. So the target is proven
reachable with competition-grade compute, and we are ~2x off the front, not outclassed.

### 20.2 The cap: not binding, and our own README already said so
Sim ceiling 30,000 ticks (score 3,000). The 600 s accumulated-wait cap buys 33,000-66,000 ticks at our
measured 9-18 ms/tick, so 18,000 ticks (162-324 s of wait) fits. It has bound us exactly once, at 110 ms/tick
during the tunnel era (attempt #3, 580.96 + "bottleneck" error). User's instinct confirmed: the cap is not
the problem; the agents die.

### 20.3 NEW MEASUREMENT (experiments/w_traits.py, seed 100, deployed E3 params, macOS)
119 births in 7,292 ticks (the run died at 7,292 - the usual midgame wall). Newborn traits vs the default
genotype: **max_energy mean 589.7 / max 996.8** (default 500, cap 1,000 - i.e. the population already drifts
to ~2x the cap by accident), hearing mean 52.0 / max 68.6 (default 50, cap 100), speed mean 10.2 / max 14.6
(default 10, cap 20), sprint mean 19.7 (default 20), **vision mean ~200 / max 257.8 (default 200, cap 400 -
essentially unmoved)**. Fusion: the sim mutates 6 heritable traits at 10 %/birth by U(0.5,1.5) up to 2x the
starting value, the genome of every agent is visible in the payload, and the policy chooses who spawns - so
**reproduction is a directed-evolution channel nobody has steered.** Nothing selects for vision, which is why
the one trait that attacks the binding constraint (ACCESS: agents blind 76-87 % of ticks) has not moved.
Predators on the same run: 0 -> 2 (t~300 s) -> 3 (t~600) -> 5 (t~700), consistent with the (1/N)*dt*t*1e-4
law (~4 at t=900 s, ~16 at t=1,800 s); predators are NEVER removed, so the endgame is an avoidance race
against rising density while production decays 0.5^(t/300).

### 20.4 Deliverables written
- `SPEC_PARAMETERS.md` - every knob in three layers (grader / world / controller) with line references.
- `PIVOT_V2_SENSORS_AND_SELECTION.md` - all insights, all constraints, the paid-for dead ends, the pivot
  ("upgrade the sensors, shrink the swarm": genome read + breeder selection + retested swarm size + repriced
  commit search), the honest ML verdict, the pre-registered arms E-V2-0..4 with bars, and the 4 decisions
  needed from the user (chief among them VPS access and whether to pause the residual-RL trainer, which is
  holding ~600 % CPU on the Mac).
Next action (on approval): E-V2-0 mechanism gate - genome telemetry + breeder selection OFF/ON, 20 paired
seeds, horizon 18,000, on Linux. ETA ~40 min diagnostic + ~70 min per 2-lane block.


### 19.7 RETRACTION: W1's "+35% ML signal" was chaos, not learning (2026-09-18 21:32Z)
I reported at 21:30 that the residual policy's eval (9,355 / 9,666 ticks vs the base's 7,180) was "the first
substantial ML signal". The delta probe (w_res_delta_probe.py, w_res_delta_probe on the it-8 checkpoint,
63,869 decisions) refutes that:
```
applied |delta dist frac|  mean 0.00166  p90 0.00272   (the bound allows 0.20 = 20%)
applied |delta dir| rad    mean 0.00449  p90 0.00872   (the bound allows 0.60 rad)
applied |delta turn| rad   mean 0.00127
decisions with |delta dir| < 0.01 rad (effectively untouched): 92.8%
```
The correction is 0.17% of the move distance and a quarter of a degree of steering, and the evaluation is
GREEDY - so the evaluated policy is the base plus a microscopic nudge. In a chaotically sensitive simulator
that is enough to route a trajectory completely differently (the residual's per-seed values
[11555, 9158, 8287] share nothing with the base's [7377, 4055, 10107]), which is exactly what the observed
swings are: luck, with no mechanism. The 20-seed confirmation was killed because it had stopped measuring a
policy and started measuring a lottery. THE TALE I MISSED: the checkpoint's head norms (|wo| = 0.0011)
already said the correction was near zero; I noted them but still used the eval number.

WHY THE POLICY DID NOT LEARN (concrete, fixable):
1. The reward is the raw score delta = +0.1/tick constant + fruit/1000 - predation/100. Batch advantage
   normalisation removes the constant, but what remains is a very weak local signal spread over a
   12,000-tick episode: "survive 3,000 ticks longer" is a global outcome and GAE(gamma 0.999) credits only
   ~1,000 steps. This project already solved this once - rl_fleet.py had `shaped=True`, subtracting the
   constant per-step term so only the differentiators drive learning. I dropped that when writing
   rl_residual.py.
2. The evaluation metric is unusable for early stopping: 3 seeds of a chaotic quantity cannot distinguish a
   real gain from noise, which is why "it 8 was best, it 12/16 degraded" is itself probably noise. This
   project's own lesson applies: judge learning on a SMOOTH proxy (income per tick, which is stable:
   fruit eaten came in at 1,312/1,481/1,873/1,594/1,426 across seeds) and only verify ticks at the end.
3. Exploration sigma in the raw space (0.06/0.10/0.05) times the bounds is a ~0.06 rad steering nudge -
   large enough to explore, but the MEAN never moved, which is the signature of a gradient too weak or too
   noisy to climb, not of a policy that explored and found nothing better.

If the fixes still leave the mean at ~zero, the honest conclusion is that this action space is already at a
local optimum for the heuristic and a BOUNDED local correction cannot improve it - which would push the
remaining effort onto the thin relay (W2), the biome axis, or an unbounded (from-scratch) learner.


---

## 20. THE PIVOT: judging eight from-scratch ideas against our own measurements (2026-09-18 21:43Z)

The user asked for a genuinely different research program, not more heuristic variants, and supplied a list
of eight from-scratch directions. Judged against today's measurements rather than in the abstract:

ALREADY DONE OR ALREADY DEAD
- #5 potential-field: that IS the current controller (39 evolved coefficients, plateaued).
- #4 evasion-first: partly banked - E3 (+9.5%, 13/20, now live) is exactly this idea. But predation is only
  23-41% of deaths against starvation's 32-51%, and 92-98% of predation deaths are agents below the
  100-energy sprint lockout, so evasion and energy are ONE problem, not two.
- #7 self-play predators: the grader's predators are a FIXED scripted policy, so training against a learned
  adversary optimizes against an opponent that never appears at grading time.
- #8 world models: we own the true simulator and it is deterministic; a learned model adds error where
  ground truth is free, and only pays if planning must be cheap AT SERVING TIME, which the 33 ms/tick
  budget forbids.
- #3 MPC: structurally different but arithmetically impossible as the controller - 8 candidates x 20 steps
  x 5-16 agents is 800-2,500 simulated fleet-ticks per real tick (100-300x: a run becomes ~26 hours), and
  it blows the serving latency budget by two orders of magnitude. KEPT AS AN ORACLE (bet C): offline, with
  1-3 agents, it answers whether 1,800 is reachable by ANY policy and can label distillation data.
- #5/#6 note: centralization is FREE at serving time - the grader already sends every agent's state in one
  request - which is what makes the centralized bet legal.

THE THREE BETS TAKEN
- A. Evolved policy network from scratch (940 params, pop 16) with a PROXY fitness. Launched overnight.
- B'. Centralized fleet-level META-controller: a small evolved policy over the GLOBAL state that modulates
  the heuristic's fleet-level modes (population target, spawn aggression, banking, thinning) while per-agent
  actions stay the heuristic + E3. This is the better-shaped version of #6 for our remaining compute: tiny
  search space, free at serving time, and it targets exactly the structural gap we measured (relay timing
  and role allocation), which 39 independent scalars cannot express.
- C. MPC oracle: the reachability ceiling for lookahead, cheap because it is 1-3 agents in endgame states.

TWO MEASUREMENT FACTS THAT SHAPED ALL OF THIS
1. Proxy fitness is MANDATORY, not optional. A candidate that gained +25% on its own 3-seed pool measured
   -2.8% (8/20 seeds) on held-out data: few-seed selection on a chaotic objective selects noise. Income is
   stable (+-12% across seeds) while ticks span 640..12,785, so fitness = fruit energy per available
   agent-slot, with ticks for reporting only.
2. The first version of that proxy was itself gameable - it gave its best score to a policy that survived
   127 ticks (a rate over a tiny denominator). Caught in the smoke test and fixed by dividing by the
   horizon instead of by realised agent-ticks, so a dead fleet scores ~0.

HONEST EXPECTATION for bet A: a random-init net survives 127 ticks against the heuristic's ~7,600, and the
winning recipe in the closest published competition used ~20 BILLION steps against our ~10 million. Bet A
is a genuine from-scratch experiment and a background long shot, not the likely winner tonight. Bet B' is
where the remaining leverage actually is, because it searches a small space that our measurements say
contains the missing mechanism.

## §21. W2 thin-relay / banking — REFUTED (and a baseline error caught)

**Two runs, both analysed.**

Run 1 (4 arms, seeds 1300-1301, 18k horizon) used `wB0_ref_h1.json` as the reference. That file
contains **zero `evade_*` keys** while the live controller carries all five, so the whole run
compared the arms against **pre-E3 H1** — a baseline ~10% weaker than what is serving traffic.
Same class of error as the phase-test baseline earlier (`/opt/nac-phase-test/deployed_params.json`).
All wB configs rebuilt on top of the live controller; the new reference verifies **key-by-key
identical to `best_controller/params.json`** (mismatches: NONE).

Run 1 numbers, for the record only (invalid baseline): wB1 (arm 3000 / keep 2 / 70%) -38%,
wB2 (4500 / 2 / 75%) -24%, wB3 (6000 / 1 / 85%) +0.5%.

Run 2 (corrected baseline, ref + wB3, fresh seeds 1400-1403, 18k):
- seed 1400  ref 5807 | wB3 5807   armed 0    (identical — fleet died before the 6000 trigger)
- seed 1401  ref 5243 | wB3 5243   armed 0    (identical)
- seed 1402  ref 9523 | wB3 6810   armed 3733  **-28.5%** (fruit_ema 0.021)
- seed 1403  ref 5606 | wB3 5606   armed 0    (identical)

The one seed where the mode actually fired lost 28%: thinning to 1 agent cut income more than the
relay bought back. Combined with run 1's -24%/-38% arms, the direction is negative in every run.
**Verdict: no evidence of gain; parked.** Not worth 20 seeds. Caveat stated honestly: only ONE
armed seed in the corrected run, so this is "insufficient and unfavourable", not a tight estimate.

Third observation, unplanned but load-bearing: **3 of the 4 fresh seeds died before tick 6,000**
(5807/5243/5606) and the fourth reached 9523. Live E3 reference mean over 4 fresh seeds = 6545
(score ~655), range 5243-9523 — i.e. the modal outcome of a *good* policy on unseen seeds is
mid-game extinction around 5-6k ticks, with a fat right tail. That is 655 vs our official 798,
consistent (mean-of-3 on favourable preset seeds). It also says the endgame/late-game ideas can
only ever pay off on the minority of seeds that reach the late game — which is exactly why the
thin-relay trigger (tick >= 6000) fired on 1 of 4.

## Discipline family scoreboard (updated)
| knob family | result |
|---|---|
| reserve_frac / repro energy floors (lockout) | refuted, 4/4 arms -9% to -36% |
| memory / follow / ARS search | refuted, -18% to -50% |
| thin relay / banking | refuted, -24% to -38% (one armed seed -28.5%) |

The recurring mechanism: every "discipline" knob trades away income in a world that is
**access-limited, not energy-limited**, so it cannot be net-positive. Four independent
refutations of one family is a real finding: stop shopping here.

*Written 2026-09-18 22:08Z*
